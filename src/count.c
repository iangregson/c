#include <stdio.h>

/* count the characters from stdin */

int main() {

  double nc;

  // nc = 0;

  // while (getchar() != EOF) {
  //   ++nc;
  // }

  for (nc = 0; getchar() != EOF; ++nc) {
    
  }

  printf("%.0f\n", nc);
}